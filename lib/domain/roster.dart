/// Rostersコンポーネントで使用する型定義
class Roster {
  Roster(this.imageFile, this.position, this.number, this.name, this.rating);

  String imageFile;
  String position;
  int number;
  String name;
  int rating;
}